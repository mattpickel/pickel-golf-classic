services:
  - type: web
    name: pickel-golf-classic-api
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: WEBHOOK_SECRET
        sync: false
      - key: DATABASE_URL
        sync: false
      - key: NODE_ENV
        value: production
